# API Keys
# Get your key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# OCR Settings
USE_GOOGLE_VISION=false
GOOGLE_APPLICATION_CREDENTIALS=

# Application Settings
UPLOAD_DIR=./storage/uploads
PROCESSED_DIR=./storage/processed
LOG_DIR=./storage/logs
MAX_FILE_SIZE=50
ALLOWED_EXTENSIONS=pdf
MAX_CONCURRENT_PROCESSING=5

# Server Settings
HOST=0.0.0.0
PORT=8000

# Google Drive OAuth (for connector integration)
# Create OAuth credentials at: https://console.cloud.google.com/apis/credentials
# 1. Create a new project or select existing one
# 2. Enable Google Drive API
# 3. Create OAuth 2.0 credentials (Web application type)
# 4. Add http://localhost:8000/api/connectors/google-drive/oauth-callback as authorized redirect URI
# 5. Copy the Client ID and Client Secret here
GOOGLE_OAUTH_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=your-client-secret
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:8000/api/connectors/google-drive/oauth-callback
